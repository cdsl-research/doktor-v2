apiVersion: apps/v1
kind: Deployment
metadata:
  name: front-app-deploy
  namespace: front
  labels:
    app: front
spec:
  replicas: 3
  selector:
    matchLabels:
      app: front
  template:
    metadata:
      annotations:
        sidecar.istio.io/status: ""
      labels:
        app: front
    spec:
      containers:
        - name: front-app-container
          image: ghcr.io/cdsl-research/front:master-0fc87cd
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: SERVICE_PAPER_HOST
              value: paper-app.paper
            - name: SERVICE_PAPER_PORT
              value: "4000"
            - name: SERVICE_AUTHOR_HOST
              value: author-app.author
            - name: SERVICE_AUTHOR_PORT
              value: "4000"
---
apiVersion: v1
kind: Service
metadata:
  name: front-app
  namespace: front
spec:
  type: NodePort
  selector:
    app: front
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 8000
      nodePort: 30200
