apiVersion: apps/v1
kind: Deployment
metadata:
  name: paper-app-deploy
  namespace: paper
  labels:
    app: paper
spec:
  replicas: 3
  selector:
    matchLabels:
      app: paper
  template:
    metadata:
      labels:
        app: paper
    spec:
      containers:
        - name: paper-app-container
          image: ghcr.io/cdsl-research/paper:master-c5c8099
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: MONGO_HOST
              value: paper-mongo
            - name: MINIO_HOST
              value: "paper-minio.paper.svc.cluster.local:9000"
---
apiVersion: v1
kind: Service
metadata:
  name: paper-app
  namespace: paper
spec:
  type: ClusterIP
  selector:
    app: paper
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 8000
