version: "3.9"

services:
  front-admin:
    build: .
    command: [ "main:app", "--reload", "--host", "0.0.0.0" ]
    restart: always
    volumes:
      - .:/work
    ports:
      - "4300:8000"
    environment:
      - SERVICE_PAPER_HOST=paper-app
      - SERVICE_PAPER_PORT=8000
      - SERVICE_AUTHOR_HOST=author-app
      - SERVICE_AUTHOR_PORT=8000
    networks:
      - frontend

networks:
  frontend:
    external: true
